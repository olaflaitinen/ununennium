# Scene Classification

Multi-label or single-label image classification.

## Example

```python
from ununennium.models import create_model

model = create_model("resnet50_classifier", in_channels=12, num_classes=10)
```
